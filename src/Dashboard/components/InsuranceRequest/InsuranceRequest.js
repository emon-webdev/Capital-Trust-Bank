import React from 'react';
import useTitle from '../../../hooks/useTitle/useTitle';

const InsuranceRequest = () => {
    useTitle("InsuranceRequest")
    return (
        <div>
            <h1>Insurance Request</h1>
        </div>
    );
};

export default InsuranceRequest;